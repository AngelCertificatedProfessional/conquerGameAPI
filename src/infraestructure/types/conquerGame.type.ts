export const CONQUERGAMEPARTIDA = Object.freeze({
    LOBBY: 1,
    AGREGARPIEZASTABLERO: 2,
    JUEGOINICIADO: 3,
    FINALIZADO: 4,
    CANCELADO: 5,
})

export const ACCIONTIPOJUEGO = Object.freeze({
    INIDIVIDUAL: 1,
    EQUIPO: 2,
})

export const JUGADORESARREGLO = Object.freeze(["O", "B", "R", "P", "G", "Y"])
